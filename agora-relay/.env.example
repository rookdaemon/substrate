# Agora Relay — Environment Variables

# Required: Secret used to sign JWT session tokens.
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
AGORA_RELAY_JWT_SECRET=your-32-plus-byte-random-secret-here

# Optional: JWT token expiry in seconds (default: 3600 = 1 hour)
# Agents must re-register after their token expires.
AGORA_JWT_EXPIRY_SECONDS=3600

# Optional: Port for the WebSocket relay server (default: 3001)
# The REST API runs on PORT+1 (default: 3002)
PORT=3001
